<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Unit 2 Assessment</title>
</head>
<body>
  <h1>Book List!</h1>
  <a href="books/new">Add a New Book</a>
  <% if (books.length) {%>
    <table>
      <thead>
        <tr>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% books.forEach(book=> { %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.read ? ' Read' : ' Not Read' %></td>
          <td><form action="/books/<%= book._id %>?_method=DELETE" method="POST">
          <button id="delBtn">Delete</button></td>
        </form>
        <% }) %>
      </tbody>
    </table>
</h3>
    <% } else { %>
      <h2>Looks like you have no books!</h2>
      <% } %>
</body>
</html>